# ComputerPilot MCP v0.3.0 Release Validation

Date: 2026-09-25

## Scope

v0.3.0 is the ComputerPilot MCP rebrand release. It changes public naming, package/release metadata, artifact prefixes, discovery-oriented README content, and repository metadata while preserving the protocol-level MCP server identifier for compatibility.

## Compatibility invariant

`core/config.py` must continue to expose:

`server_name = "ali_windows_agent_mcp"`

The public MCP display title may change to ComputerPilot MCP, but existing client/tunnel configuration must not require an identifier migration.

## Local validation

The rebrand release-preparation tree passed:

- startup check: v0.3.0, 112 tools, unique tool names;
- focused tunnel/Doctor/package tests: 63 passed;
- full pytest suite: passed with only expected platform/optional skips;
- Ruff: zero violations;
- mypy: zero issues across 193 source files;
- compileall: passed;
- MCP health: import/startup/tool-registration/filesystem/terminal checks passed;
- Windows platform validation: passed on Python 3.12.8, including clean launcher validation, full Doctor, quick benchmark, a 60-workflow restart/reconciliation soak, three successful durable jobs, zero leaked leases, zero orphan operations, and zero orphan events;
- deterministic packaging: all five v0.3.0 targets built with the `computerpilot-mcp-v0.3.0` prefix;
- Windows release manifest: `project = "computerpilot-mcp"`, version `0.3.0`, managed tunnel runtime enabled, first-use download enabled;
- Windows package inventory: zero vendored tunnel-client/Cloudflared runtime entries.

## GitHub and publication gates

Before publication:

- exact-head GitHub CI, Platform Release Validation, and Release Packaging Smoke must pass on the release-preparation commit;
- the final release-trigger commit must repeat those same exact-head gates;
- the published GitHub release must target the final release SHA;
- the published Windows artifact must be inspected again after publication.

## Repository migration

After the code/release preparation is merged, the GitHub repository is renamed from `dibbed/windows-agent-mcp` to `dibbed/computerpilot-mcp`. The local `origin` remote is then updated to the canonical new URL and verified against `main`.
